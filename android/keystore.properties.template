# Android 签名配置
# 将此文件复制为 keystore.properties 并填入实际值
# 注意：不要将 keystore.properties 提交到版本控制

# Keystore 文件路径
storeFile=keystore.jks

# Keystore 密码
storePassword=your_store_password

# Key 别名
keyAlias=your_key_alias

# Key 密码
keyPassword=your_key_password
